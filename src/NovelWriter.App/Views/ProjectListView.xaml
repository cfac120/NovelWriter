<UserControl x:Class="NovelWriter.App.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="我的项目" FontSize="14" FontWeight="SemiBold"
                       Foreground="#A6ADC8" Margin="0,0,0,8"/>
            <Button Content="+ 创建项目"
                    Command="{Binding CreateProjectCommand}"
                    Foreground="#89B4FA" Background="#313244"
                    HorizontalAlignment="Left" Padding="16,6"/>
        </StackPanel>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Projects}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,0,4" Padding="8" Background="#2A2A3C"
                            CornerRadius="4" Cursor="Hand"
                            MouseLeftButtonDown="ProjectClicked" Tag="{Binding Id}">
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" Foreground="#CDD6F4" FontSize="13"/>
                            <TextBlock Foreground="#6C7086" FontSize="11">
                                <Run Text="{Binding Genre}"/><Run Text=" · "/>
                                <Run Text="{Binding ChapterCount}"/><Run Text="章"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>

<Window x:Class="NovelWriter.App.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认记忆变更" Height="500" Width="650"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E2E"
        ResizeMode="CanResizeWithGrip">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="记忆变更确认" FontSize="16" FontWeight="Bold"
                   Foreground="#89B4FA" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" Text="以下变更需要您的确认。请逐项审批或使用批量操作。"
                   Foreground="#A6ADC8" FontSize="12" Margin="0,0,0,12"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ConfirmationList" Margin="0,0,0,8">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,6" Padding="10,8" CornerRadius="4"
                                Background="{Binding ItemBackground}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Width="4" CornerRadius="2"
                                        Background="{Binding TypeColor}" Margin="0,0,10,0"
                                        VerticalAlignment="Stretch"/>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding TypeLabel}" Foreground="{Binding TypeColor}"
                                               FontSize="11" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Summary}" Foreground="#CDD6F4"
                                               FontSize="12" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                                    <Button Content="通过" Width="50" Height="28"
                                            Foreground="#A6E3A1" Background="#313244" BorderThickness="0"
                                            Click="ApproveItem_Click" Tag="{Binding}"
                                            ToolTip="通过" Margin="0,0,4,0" FontSize="11"/>
                                    <Button Content="拒绝" Width="50" Height="28"
                                            Foreground="#F38BA8" Background="#313244" BorderThickness="0"
                                            Click="RejectItem_Click" Tag="{Binding}"
                                            ToolTip="拒绝" FontSize="11"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="全部通过" Click="ApproveAll_Click"
                    Foreground="#A6E3A1" Background="#313244"
                    BorderThickness="0" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="全部拒绝" Click="RejectAll_Click"
                    Foreground="#F38BA8" Background="#313244"
                    BorderThickness="0" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="完成" Click="Done_Click"
                    Foreground="#89B4FA" Background="#313244"
                    BorderThickness="0" Padding="12,6" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>

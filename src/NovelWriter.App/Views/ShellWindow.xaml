<Window x:Class="NovelWriter.App.Views.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:NovelWriter.App.ViewModels"
        Title="NovelWriter" Height="800" Width="1400"
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- === 菜单栏 === -->
        <Menu Grid.Row="0" Background="#11111B" Foreground="#A6ADC8" FontSize="12">
            <MenuItem Header="文件">
                <MenuItem Header="新建项目" Click="NewProject_Click" Foreground="#A6E3A1"/>
                <MenuItem Header="打开项目" Click="OpenProject_Click"/>
                <Separator/>
                <MenuItem Header="退出" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="风格库">
                <MenuItem Header="从目录导入风格" Click="ImportStyles_Click"/>
                <MenuItem Header="管理风格库" Click="ManageStyles_Click"/>
            </MenuItem>
            <MenuItem Header="AI审查" IsEnabled="False"/>
            <MenuItem Header="发布" IsEnabled="False"/>
        </Menu>

        <!-- === 三栏主体 === -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="340"/>
            </Grid.ColumnDefinitions>

            <!-- 左栏：资源管理器 -->
            <Border Grid.Column="0" Background="#181825" BorderBrush="#313244" BorderThickness="0,0,1,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>

                        <!-- 小说内容(标题动态替换) -->
                        <Expander x:Name="ContentExpander" Header="NovelWriter" IsExpanded="True" Margin="4,4,4,0">
                            <Expander.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock x:Name="ContentHeader" Text="NovelWriter" FontSize="13"
                                               FontWeight="SemiBold" Foreground="#89B4FA"/>
                                </DataTemplate>
                            </Expander.HeaderTemplate>
                            <StackPanel Margin="8,4">
                                <TextBlock x:Name="SynopsisFile" Text="梗概" Foreground="#6C7086" FontSize="12"
                                           Margin="0,2" Cursor="Hand" MouseLeftButtonDown="OpenFileNode" Tag="synopsis"/>
                                <TextBlock x:Name="OutlineFile" Text="大纲" Foreground="#6C7086" FontSize="12"
                                           Margin="0,2" Cursor="Hand" MouseLeftButtonDown="OpenFileNode" Tag="outline"/>
                                <TreeView x:Name="ChapterTree" Background="Transparent" BorderThickness="0"
                                          Foreground="#CDD6F4" Margin="0,4,0,8"
    
                                      SelectedItemChanged="ChapterTreeSelected"/>
                                <TextBlock Text="L3 记忆" Foreground="#89B4FA" FontSize="12" Margin="0,8,0,4"/>
                                <TextBlock x:Name="CharFile" Text="人物档案" Foreground="#6C7086" FontSize="11"
                                           Margin="8,2" Cursor="Hand" MouseLeftButtonDown="OpenFileNode" Tag="characters"/>
                                <TextBlock x:Name="WorldFile" Text="世界观" Foreground="#6C7086" FontSize="11"
                                           Margin="8,2" Cursor="Hand" MouseLeftButtonDown="OpenFileNode" Tag="world"/>
                            </StackPanel>
                        </Expander>

                        <!-- 插曲库 -->
                        <Expander Header="插曲库" IsExpanded="False" Margin="4,8,4,0">
                            <Expander.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Text="插曲库" FontSize="12" FontWeight="SemiBold" Foreground="#FAB387"/>
                                </DataTemplate>
                            </Expander.HeaderTemplate>
                            <TreeView x:Name="InterludeTree" Background="Transparent" BorderThickness="0"
                                      Foreground="#A6ADC8" FontSize="11" Height="150"
                                      SelectedItemChanged="InterludeSelected"/>
                        </Expander>

                        <!-- 风格库 -->
                        <Expander Header="风格库" IsExpanded="False" Margin="4,8,4,8">
                            <Expander.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Text="风格库" FontSize="12" FontWeight="SemiBold" Foreground="#A6E3A1"/>
                                </DataTemplate>
                            </Expander.HeaderTemplate>
                            <StackPanel Margin="4">
                                <Button Content="+ 导入风格" FontSize="11" Foreground="#89B4FA"
                                        Background="#2A2A3C" BorderThickness="0" Padding="6,3"
                                        Click="ImportStyles_Click"/>
                                <ListBox x:Name="StyleList" Background="Transparent" BorderThickness="0"
                                         Foreground="#A6ADC8" FontSize="11" Height="150" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Expander>

                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- 中栏：标签式编辑区 -->
            <Border Grid.Column="1" Background="#1E1E2E">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 标签栏 -->
                    <TabControl x:Name="EditorTabs" Grid.Row="0" Background="#181825"
                                BorderBrush="#313244" BorderThickness="0,0,0,1" Height="32"
                                SelectionChanged="TabChanged">
                        <TabControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Header}" FontSize="11" Foreground="#CDD6F4"
                                               VerticalAlignment="Center"/>
                                    <Button Content="×" FontSize="10" Foreground="#6C7086"
                                            Background="Transparent" BorderThickness="0" Padding="4,0"
                                            Click="CloseTab_Click" Tag="{Binding}"
                                            VerticalAlignment="Center" Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </TabControl.ItemTemplate>
                    </TabControl>

                    <!-- 内容区 -->
                    <Grid Grid.Row="1">
                        <!-- 默认欢迎页 -->
                        <StackPanel x:Name="WelcomePanel" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="NovelWriter" FontSize="28" FontWeight="Bold"
                                       Foreground="#89B4FA" HorizontalAlignment="Center"/>
                            <TextBlock Text="AI 网文写作助手" FontSize="16"
                                       Foreground="#6C7086" HorizontalAlignment="Center" Margin="0,8,0,20"/>
                            <Button Content="新建项目" Click="NewProject_Click" Width="160" Height="36"
                                    Foreground="#1E1E2E" Background="#A6E3A1"
                                    BorderThickness="0" FontSize="14" HorizontalAlignment="Center"/>
                            <Button Content="打开项目" Click="OpenProject_Click" Width="160" Height="36"
                                    Foreground="#89B4FA" Background="#313244"
                                    BorderThickness="0" FontSize="14" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                        </StackPanel>

                        <!-- 编辑器 -->
                        <TextBox x:Name="ContentEditor" AcceptsReturn="True" TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 Foreground="#CDD6F4" Background="#1E1E2E"
                                 BorderThickness="0" Padding="16" FontSize="14"
                                 FontFamily="Consolas, Microsoft YaHei" Visibility="Collapsed"
                                 TextChanged="ContentEditor_Changed"/>
                    </Grid>
                </Grid>
            </Border>

            <!-- 右栏：AI 面板 -->
            <Border Grid.Column="2" Background="#11111B" BorderBrush="#313244" BorderThickness="1,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 阶段指示器 + 操作按钮 -->
                    <Border Grid.Row="0" Background="#181825" Padding="10" BorderBrush="#313244" BorderThickness="0,0,0,1">
                        <StackPanel>
                            <TextBlock Text="AI 写作进度" FontSize="13" FontWeight="SemiBold"
                                       Foreground="#89B4FA" Margin="0,0,0,8"/>
                            <ItemsControl x:Name="StageList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="0,2">
                                            <TextBlock Text="{Binding Status}" Foreground="{Binding Color}" FontSize="15" Width="22"/>
                                            <TextBlock Text="{Binding Name}" Foreground="{Binding Color}" FontSize="12" VerticalAlignment="Center"/>
                                            <Button x:Name="StageRetry" Content="重写" FontSize="10"
                                                    Foreground="#FAB387" Background="Transparent" BorderThickness="0"
                                                    Padding="4,0" Cursor="Hand" Tag="{Binding}"
                                                    Click="StageRetry_Click" Visibility="Collapsed"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <!-- 开始创作按钮(新建/打开后显示) -->
                            <Button x:Name="StartAiBtn" Content="▶ 开始辅助创作" Height="32" Margin="0,8,0,0"
                                    Foreground="#1E1E2E" Background="#89B4FA" BorderThickness="0" FontSize="11"
                                    Click="StartAiBtn_Click" Visibility="Collapsed"/>

                            <!-- 阶段确认/重写按钮(创作中显示) -->
                            <StackPanel x:Name="StageActions" Orientation="Horizontal" Margin="0,8,0,0" Visibility="Collapsed">
                                <Button x:Name="ConfirmBtn" Content="✓ 确认" Width="80" Height="28"
                                        Foreground="#1E1E2E" Background="#A6E3A1" BorderThickness="0" FontSize="11"
                                        Click="StageConfirm_Click" Margin="0,0,8,0"/>
                                <Button x:Name="RewriteBtn" Content="↻ 重写" Width="80" Height="28"
                                        Foreground="#1E1E2E" Background="#FAB387" BorderThickness="0" FontSize="11"
                                        Click="StageRewrite_Click"/>
                            </StackPanel>
                            <!-- "浏览模式" 提示 -->
                            <TextBlock x:Name="BrowseHint" Text="正在浏览项目内容..." Foreground="#6C7086"
                                       FontSize="10" Margin="0,8,0,0" Visibility="Collapsed" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <!-- 对话区 -->
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ScrollViewer x:Name="ChatScroll" VerticalScrollBarVisibility="Auto" Background="#11111B">
                            <ItemsControl x:Name="ChatList" Margin="8">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding Bg}" Padding="8,6" Margin="0,0,0,4" CornerRadius="4">
                                            <TextBlock Text="{Binding Text}" FontSize="11" Foreground="{Binding Fg}"
                                                       TextWrapping="Wrap"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <Border Grid.Row="1" Background="#181825" Padding="6" BorderBrush="#313244" BorderThickness="0,1,0,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBox x:Name="ChatInput" AcceptsReturn="True" TextWrapping="Wrap"
                                         MinHeight="50" MaxHeight="120"
                                         Foreground="#CDD6F4" Background="#2A2A3C" BorderBrush="#45475A"
                                         Padding="6" FontSize="11" VerticalScrollBarVisibility="Auto"/>
                                <Button Grid.Row="1" Content="发送 (Ctrl+Enter)" Height="26"
                                        Foreground="#89B4FA" Background="#313244" BorderThickness="0"
                                        FontSize="10" Click="ChatSend_Click" Margin="0,4,0,0"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- LLM 配置 -->
                    <Border Grid.Row="2" Background="#181825" Padding="8" BorderBrush="#313244" BorderThickness="0,1,0,0">
                        <StackPanel>
                            <TextBlock Text="LLM 配置" FontSize="11" FontWeight="SemiBold"
                                       Foreground="#6C7086" Margin="0,0,0,4"/>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Key" Foreground="#6C7086" FontSize="10" VerticalAlignment="Center"/>
                                <TextBox x:Name="ApiKeyBox" Grid.Column="1" Height="22" FontSize="10"
                                         Foreground="#CDD6F4" Background="#2A2A3C" BorderBrush="#45475A" Padding="4"/>
                            </Grid>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="模型" Foreground="#6C7086" FontSize="10" VerticalAlignment="Center"/>
                                <TextBox x:Name="ModelBox" Grid.Column="1" Height="22" FontSize="10"
                                         Text="deepseek-v4-pro"
                                         Foreground="#CDD6F4" Background="#2A2A3C" BorderBrush="#45475A" Padding="4"/>
                            </Grid>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="URI" Foreground="#6C7086" FontSize="10" VerticalAlignment="Center"/>
                                <TextBox x:Name="UriBox" Grid.Column="1" Height="22" FontSize="10"
                                         Text="https://api.deepseek.com/v1/chat/completions"
                                         Foreground="#CDD6F4" Background="#2A2A3C" BorderBrush="#45475A" Padding="4"/>
                            </Grid>
                            <Button Content="保存并连接" Height="26" Margin="0,4,0,0" FontSize="10"
                                    Foreground="#A6E3A1" Background="#313244" BorderThickness="0"
                                    Click="SaveLlmConfig_Click"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
